# SR-PPO-DLS Source Map (token-safe reference)

Read this instead of re-auditing the repo. Repo root: `D:\data\hoang_anh\mpdik_kassow`
(only git repo in the tree). Dataset v2 *data* lives OUTSIDE this repo, in sibling directories
under `D:\data\hoang_anh\` (e.g. `mpdik_kassow_v2_work`, `mpdik_kassow_v2_eval_devval`,
`mpdik_kassow_v2_eval_frozen`, `mpdik_kassow_v2_eval_public`, `KR810_Tier0_Tier4_Dataset_v2.0.0`)
— always pass an explicit `dataset_root`, never assume CWD or repo root. `dataset_v2/` in-repo is
code only (locator/generation/validation), not data.

| Chủ đề | File | Symbol/function/class | Vai trò |
|---|---|---|---|
| FK | `kinematics/forward_kinematics.py` | `forward_kinematics(model_context, q, data=None) -> FKResult` | MuJoCo `mj_forward` + site pose read; returns position/rotation_matrix/quaternion_wxyz |
| Jacobian | `kinematics/jacobian.py` | `geometric_jacobian_world(model_context, q, data=None) -> (6,7)`; `finite_difference_jacobian_world`; `jacobian_relative_error` | World-frame geometric Jacobian via `mj_jacSite`, plus FD cross-check |
| Pose error | `kinematics/pose_error.py` | `position_error_vector`; `orientation_error_vector_world(R_target,R_current)`; `full_pose_error`; `weighted_pose_error(vec,pos_w,ori_w)` | Position error + SO(3) log-map orientation error, world/left convention |
| DLS solver (LOCKED) | `kinematics/dls_solver.py` | `dls_single_update(model_context,q,target_position,target_rotation,config,data=None)->DLSStepResult`; `solve_dls_until_converged(model_context,q_init,target_position,target_rotation,config=None,record_history=False)->DLSResult` | Core damped-least-squares step/solve. Never modify. `dls_single_update` is the probe fn for observation §6.4 |
| Adaptive damping | `kinematics/adaptive_damping.py` | `compute_adaptive_damping(sigma_min,threshold,lambda_min,lambda_max)->float` | λ(q) schedule near singularity |
| Singularity | `kinematics/singularity_metrics.py` | `singular_values`, `minimum_singular_value`, `maximum_singular_value`, `numerical_rank(J,tol)`, `condition_number(J,safe_floor)`, `is_near_singular(J,threshold)` | Feeds observation σ_min/condition-number features |
| Joint limits | `kinematics/joint_limit_utils.py` | `validate_joint_vector`, `clip_to_operational_limits`, `operational_limit_violation_mask`, `normalized_joint_limit_margin`, `minimum_joint_limit_margin`, `joint_center`, `joint_centering_gradient` | Margins for observation + reward penalty `m(q)`; DO NOT modify — SR-PPO-DLS projection needs its own margin table, call these read-only |
| Manipulability | `kinematics/manipulability.py` | `yoshikawa_manipulability(J)`, `positional_manipulability(J)`, `normalized_jacobian(J,scale)` | Not directly required by PDF observation but available |
| Model/robot loader | `kinematics/model_loader.py` | `load_model_context(model_path=None, robot_config_path=None)->ModelContext` | `ModelContext` has `.new_data()/.copy_data()/.validate_q()/.set_qpos()/.forward()`, `nq/nv/joint_names/dof_addresses/ee_site_id/operational_lower_rad/operational_upper_rad` |
| Quaternion utils | `kinematics/quaternion_utils.py` | `normalize_quaternion_wxyz`, `canonicalize_quaternion_wxyz`, `quaternion_wxyz_to_matrix`, `rotation_matrix_to_quaternion_wxyz`, `quaternion_geodesic_angle` | wxyz convention throughout dataset v2 |
| Rotation utils | `kinematics/rotation_utils.py` | `skew`, `vee`, `validate_rotation_matrix`, `project_to_rotation_matrix`, `so3_exp`, `so3_log`, `rotation_geodesic_angle` | SO(3) log/exp used by pose_error and probe |
| Point-IK DLS runner (v1, NOT for v2 eval) | `algorithms/point_dls.py` | `run_point_dls(...)->List[PointIKResult]` | v1-only, loads legacy `.npz` benchmark — do not reuse for SR-PPO-DLS Point-IK eval |
| Warm-start trajectory DLS | `algorithms/warm_start_dls.py` | `run_warm_start_dls(model_context,q_trial_initial,target_positions,target_quaternions,dls_config,fail_fast=False,show_progress=True)->List[RawWaypointSolve]` | Used by trajectory waypoint-0 secondary application (§9.5) |
| Cold-start trajectory DLS | `algorithms/cold_start_dls.py` | `run_cold_start_dls(...)` same signature | Trajectory baseline without continuity |
| Sequential trial dispatcher (v1) | `algorithms/sequential_dls.py` | `run_sequential_trial(trajectory_id,trial_id,method,...)->List[WaypointResult]` | v1 CSV-based; v2 trajectory eval uses `evaluation_v2/trajectory_eval.py` instead |
| Result dataclasses | `algorithms/result_types.py` | `PointIKResult`, `WaypointResult`, `point_ik_results_to_dataframe`, `waypoint_results_to_dataframe` | |
| Dataset v2 root resolution | `dataset_v2/locator.py` | `dataset_v2_paths(dataset_root)->DatasetV2Paths`; `require_dataset_v2_root(dataset_root)->DatasetV2Paths` | Delegates to `utils/dataset_locator.py::resolve_dataset_root`; CWD-independent, explicit-root only |
| Path resolution pattern to mirror | `utils/dataset_locator.py` | `resolve_dataset_root(dataset_root=None,*,require_exists=True)->Path`; `dataset_paths_for(root)->DatasetPaths` | `None` → v1 `REPO_ROOT` default; explicit root anchored to `Path.cwd()` then `.resolve()`d. SR-PPO-DLS path helpers should follow this pattern, parameterized, not copy v1 defaults |
| Dataset v2 scaffold | `dataset_v2/scaffold.py` | `create_dataset_v2_scaffold(dataset_root,master_seed,overwrite=False)` | Rejects v1 paths; layout: `configs/,schemas/,checksums/,tier0_validation/,tier1_point_ik/,anchors/,trajectories/{development,validation,frozen_test}/,trials/,references/,reports/` |
| Point-IK loader/generator | `dataset_v2/point_ik_generation.py` | `run_point_ik_generation(dataset_root,master_seed=None,overwrite=False,samples_per_group=None,pool_size=None,split_sizes_per_group=None,model_context=None,dry_run=False)->PointIKGenerationResult` | Produces fields listed below (leakage policy) |
| Point-IK validator | `dataset_v2/point_ik_validation.py` | (validation entry points, mirrors generation) | Independent re-check: counts, duplicate ids, hash, limits, FK-match |
| Split guard / anti-leakage | `evaluation_v2/protected_guard.py` | `is_protected_field(name)->bool`; `find_protected_fields(names)->list`; `assert_no_protected_fields(mapping,context)->None` (raises `ModelConfigurationError`) | `PROTECTED_FIELD_NAMES = {q_reference, q_reference_start, q_source_reference, q_target_reference, position_reconstruction_error_m, orientation_reconstruction_error_rad, waypoint_reachable}` + substring match. Call this (or an equivalent) from SR-PPO-DLS observation code |
| Frozen guard | `evaluation_v2/frozen_ledger.py` | `open_or_resume_frozen_access(ledger_path,*,run_id,purpose,evaluation_lock_commit,config_hash,dataset_hash,candidate_id,command,environment_fingerprint)->dict`; `mark_completed(...)`; `read_ledger(...)` | Exactly one official `run_id` may ever open `frozen_test`; raises `FrozenAccessLimitExceeded` otherwise. SR-PPO-DLS frozen run needs its OWN ledger entry — do not reuse the pure-DLS run's ledger row |
| DLS evaluation outputs (Point-IK) | `evaluation_v2/point_eval.py` | `evaluate_point_ik_split(public_point_ik_file,candidate,*,model_context=None,sample_limit=None,show_progress=False)->pd.DataFrame` | Per-sample: `sample_id,difficulty_id,converged,position_error_m,orientation_error_{rad,deg},iterations,solve_time_ms,initial/final_sigma_min,final_condition_number,minimum_joint_limit_margin,joint_limit_violation,failure_reason,success_{coarse,standard,strict},q_solution_q1..q7`. This is the pattern SR-PPO-DLS's paired evaluator should mirror (read-only reference, new module) |
| DLS evaluation outputs (trajectory) | `evaluation_v2/trajectory_eval.py` | `evaluate_trajectory_trial(public_canonical_file,*,trial_id,trajectory_id,trajectory_family,difficulty,split,q_initial,method,candidate,model_context=None,waypoint_limit=None,show_progress=False)->pd.DataFrame` | Per-waypoint rows; dispatches to `warm_start_dls`/`cold_start_dls` |
| Locked candidate configs | `evaluation_v2/candidate_configs.py` | `candidate_set()->List[CandidateConfig]`; `candidate_by_id(id)`; `CandidateConfig.solver_config()->dict`; `REPORTING_THRESHOLDS` | `cand_D_pure_dls` = selected/locked. Tiers: coarse 6mm/5°, standard 3mm/2° (primary), strict 1mm/1°. Convergence tolerance fixed at strict for every candidate |
| Config fingerprint / lock | `evaluation_v2/fingerprints.py` | `canonical_json`, `config_fingerprint(config)->sha256`, `code_fingerprint()->sha256` (over `evaluation_v2/,algorithms/,kinematics/,evaluation/`), `directory_fingerprint(root,skip_names=())`, `environment_fingerprint()` | Reuse pattern for SR-PPO-DLS's own `srpdik/sr_ppo_dls/fingerprints.py`; do not edit this file |
| Lock bundle | `evaluation_v2/lock_bundle.py` | `build_lock_bundle(selected_candidate,*,dataset_root,public_root,protected_root,development_summary,validation_summary,proposed_frozen_command)->dict`; `write_lock_bundle(path,bundle)` | `lock_status` always `"candidate_locked_pending_commit"` |
| Checksum | `dataset_v2/checksums.py` | `build_file_entry`, `build_source_config_fingerprint`, `build_generated_data_fingerprint`, `build_checksum_manifest(dataset_root)->dict`, `verify_checksum_manifest(dataset_root)->List[ChecksumMismatch]`, `content_hash_of_record(record)->str` | SHA256 per file + per-record content_hash for anti-leakage |
| Manifest | `dataset_v2/manifest.py` | `build_dataset_manifest()->dict`; `apply_{tier0,anchor,core_trajectory,challenge_trajectory,trial,point_ik}_generation_status(...)` | Each returns a copy, never mutates in place |
| Seed derivation | `dataset_v2/seeds.py` | `derive_seed(base_seed,*tags)->int` (SHA-256-based, `dataset_v2/seed/sha256/v1`); `rng_from(base_seed,*tags)->np.random.Generator` (PCG64); `canonical_seed_payload(base_seed,*tags)->bytes` | SR-PPO-DLS training-stream seeds must call this with a NEW tag namespace, not touch the file |
| Locked config values | `configs/dls_config.json` | keys: `max_iterations(100), position_success_threshold_m(0.006), orientation_success_threshold_deg(10.0), position_weight(1.0), orientation_weight(0.2), damping_mode(adaptive), lambda_default(0.01), lambda_min(1e-4), lambda_max(0.2), step_scale(1.0), max_joint_step_rad(0.1), joint_limit_avoidance(true), null_space_gain(0.02), singularity_sigma_threshold(0.03), clip_to_operational_limits(true)` | v1 config; v2 `cand_D_pure_dls` overrides some (see `candidate_configs.py`) |
| Tests: DLS solver | `tests/test_dls_solver.py` | — | Convergence/limit/damping/step-clamp behavior |
| Tests: Point-IK gen | `tests/test_dataset_v2_point_ik_generation.py` | — | Counts, determinism, no cross-split leakage, `q_target_reference != q_initial` |
| Tests: Point-IK validation | `tests/test_dataset_v2_point_ik_validation.py` | — | Independent validator catches corrupted/duplicate/out-of-limit records |
| Tests: eval harness | `tests/test_dataset_v2_eval_harness.py` | — | Public/protected isolation, Tier0 gate, point-IK/warm/cold smokes |
| Tests: frozen | `tests/test_dataset_v2_frozen.py` | — | Frozen ledger access_count==1, frozen-only export isolation |
| v1 immutable baseline | `VERSION` (=`1.0.0`), `DATASET_MANIFEST.json` | — | v1 only. Never touch, per CLAUDE.md |
| Locked pure-DLS results reference | `docs/CHATGPT_DLS_CURRENT_STATE_BRIEF.md` | — | Frozen standard success: Point-IK 95.08% (wait: brief says dev/val/frozen = 0.963/0.963/0.951 standard); trajectory warm 54.93%/cold 32.92%; 0 non-finite/171,600 solves |
| Phase history | `docs/V2_IMPLEMENTATION_LOG.md` | — | Phase 0 spec → Phase 8B frozen release (commit `151ffd76`), append-only log pattern to mirror for `SRPDIK_IMPLEMENTATION_LOG.md` |

Note on the discrepancy above: the design PDF (p.1) quotes standard Point-IK success ≈95.08%;
the current-state brief's `final_dls_summary.json` reading gave 0.951 frozen / 0.963 dev/val.
Both describe the same locked `cand_D_pure_dls` frozen artifact; treat 95.08%/95.1% as the same
number (rounding), reconcile exactly by reading `final_dls_summary.json` directly in Phase B
before locking any baseline numbers into a report.
